package pojo.integration;

/**
 * Tipo definidos de reposta que será entregue pelo Listener.
 */
public enum TypeListenerMessage {
    KEEP_ALIVE, TEXT, DATE_TIME, EVENT, CONFIG, CONFIRM, NOT_DEFINED;
}
